import { GetQuantityPipe } from './get-quantity.pipe';

describe('GetQuantityPipe', () => {
  it('create an instance', () => {
    const pipe = new GetQuantityPipe();
    expect(pipe).toBeTruthy();
  });
});
